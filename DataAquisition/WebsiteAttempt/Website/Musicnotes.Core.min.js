var Musicnotes = Musicnotes || {};
Musicnotes = Musicnotes || {};
Musicnotes.toggle = Musicnotes.toggle || {},
function() {
    function n(n) {
        if (n !== undefined && n !== null)
            $(n).on("click.toggle.musicnotes", t)
    }
    function t(n) {
        var i = $(this)
          , r = i.data("mnToggleTarget")
          , t = i.data("mnToggleClass")
          , f = i.data("mnToggleGroup")
          , u = $('[data-mn-toggle-group="' + f + '"]')
          , e = i.hasClass(t);
        r === "parent" ? (u.removeClass(t),
        u.parent().removeClass(t),
        e || (i.toggleClass(t),
        i.parent().toggleClass(t)),
        i.trigger("toggle.Musicnotes", [{
            target: r,
            attribute: t,
            group: f,
            isToggledOn: e
        }])) : (u.removeClass(t),
        $(u.data("mnToggleTarget")).removeClass(t),
        $(r).toggleClass(t),
        i.trigger("toggle.Musicnotes", [{
            target: r,
            attribute: t,
            group: f,
            isToggledOn: e
        }]));
        n.preventDefault()
    }
    this.init = n
}
.apply(Musicnotes.toggle);
Musicnotes = Musicnotes || {};
Musicnotes.toggle = Musicnotes.toggle || {};
Musicnotes.navigation = Musicnotes.navigation || {},
function() {
    function f() {
        var n = $(document);
        $(".mn-navbar-toggle")[0] != null && Musicnotes.toggle.init($(".mn-navbar-toggle")[0]);
        $(".mn-navbar-toggle").is(":visible") || t();
        $(window).resize(function() {
            $(".mn-navbar-toggle").is(":visible") ? t(!0) : t()
        });
        $(".mn-touch-toggle-mobile") != null && Musicnotes.toggle.init($(".mn-touch-toggle-mobile"));
        window.addEventListener("touchstart", function i() {
            document.getElementsByClassName("mn-touch-toggle") != null && (Musicnotes.toggle.init($(".mn-touch-toggle")),
            window.removeEventListener("touchstart", i, !1))
        }, !1);
        n.on("toggle.Musicnotes", e)
    }
    function t(n) {
        if ($(".mn-dropdown-hover").off("mouseenter", r),
        $(".mn-dropdown-hover").off("mouseleave", u),
        !n) {
            $(".mn-dropdown-hover").on("mouseenter", r);
            $(".mn-dropdown-hover").on("mouseleave", u)
        }
    }
    function e(n, t) {
        t.target !== "parent" || $(n.target).is(".hidden-desk") || ($(n.target).hasClass(t.attribute) ? $(".mn-site-overlay").addClass(t.attribute) : $(".mn-site-overlay").removeClass(t.attribute))
    }
    function r(t) {
        $(t.target).is(".hidden-desk") || (clearTimeout(n),
        n = setTimeout(function() {
            $(".mn-dropdown-hover").removeClass("open");
            $(t.currentTarget).addClass("open");
            $(".mn-site-overlay").addClass("open")
        }, i))
    }
    function u(t) {
        $(t.target).is(".hidden-desk") || (clearTimeout(n),
        n = setTimeout(function() {
            $(".mn-dropdown-hover").removeClass("open");
            $(".mn-site-overlay").removeClass("open")
        }, i))
    }
    var n = null
      , i = 180;
    this.init = f
}
.apply(Musicnotes.navigation);
